# Orbit 工作台设计记录

- 场景：个人产品经理在桌面工作环境中快速扫描 AI 状态、判断与下一步。
- 视觉：深墨黑底（#0a0e10）、面板黑（#13191b）、酸橙主信号（#c7f36b），蓝色表示进行中，珊瑚色表示提醒。
- 排版：DM Sans + Noto Sans SC；标题紧凑，数据使用 tabular-like 视觉，正文保持短段落。
- 结构：左侧全局导航；首屏问候与 AI 结论；AI 消耗/待办统计；任务、结果、知识文档；底部内嵌 AI 入口。
- 交互：导航切换、任务勾选、AI prompt mock 提交；数据层暂不持久化。
- 知识文档：本地文档页支持按项目分组、复选批量删除和单篇 Markdown 编辑/预览；可直接新建 Markdown 文档并指定项目，创建内容只存入浏览器本地库。删除有二次确认，编辑保留原始路径。
- 数据迁移：知识文档页可导出和恢复完整 IndexedDB JSON 备份，用于跨电脑迁移本地数据。
- 文档写作：新建和编辑进入独立的全屏专注页；隐藏全局导航、知识库列表和管理工具，只保留标题、项目、Markdown 编辑/预览和保存操作。
- 待办反馈：勾选未完成任务时，GSAP 为完成按钮提供弹性放大与酸橙扫光，同时平滑推进顶部任务进度条；取消完成仅回退状态，支持 `prefers-reduced-motion`。
- 总览内容卡片：最近结果改为带状态色顶边的独立结果卡，知识文档改为三列文档卡；保留原有入口和信息，减少连续列表带来的视觉平淡。
- 总览工作入口：将 AI 助手提升到待办之后的主操作区，并新增瞬记快速录入面板；快速录入复用本地 notes 保存、图片校验和完整瞬记页入口。
- 总览知识文档：文档卡改为项目文件夹语义，使用文件夹图标展示项目名、文档数量和最近更新文档，保留打开知识库入口。
- 最近结果汇报：将结果卡改为 AI 消息气泡流，使用时间线、AI 头像和状态色边框表现连续汇报，保留结果库入口。
- 色彩收敛：移除最近结果和知识文档中的蓝/珊瑚多色卡片与顶边，统一深墨灰表面，仅保留酸橙色作为状态和交互信号。
- 主题：顶栏提供跟随系统、亮色与暗色主题选择；默认跟随系统并保存本地偏好。亮色采用暖白表面、深墨文字与酸橙操作信号，覆盖总览、AI 工作台、瞬记、知识库与文档专注页。
- 亮色可读性：酸橙操作文字统一为深橄榄绿（#466d20），辅助文字统一为深灰绿（#52645c）；保留暗色模式原有酸橙亮度。
- 主题菜单：顶栏作为独立叠放层高于页面内容，避免下拉菜单被首屏动效后的内容遮挡；亮色侧栏图标使用深灰绿（#40534a），选中项使用深橄榄绿。
- 知识库亮色状态：文档行、项目组、导入状态、空状态与删除操作均使用暖白表面和深色文字；悬浮、键盘聚焦与选中使用浅绿表面，不再沿用暗色背景。
- 知识库浏览：首页仅展示最近变动的 6 篇文档及按更新时间排序的项目文件夹；项目页提供项目内搜索、最近修改/标题排序、每次加载 30 篇、批量删除和新建。编辑器保存或返回时保留所在项目上下文。
- 亮色表单：总览新增待办输入框使用深墨文字、深灰绿占位符和浅绿聚焦分隔线，避免沿用暗色文字导致不可读。
- 响应式：720px 以下导航抽屉化，统计卡单列，底部内容纵向堆叠。
- 知识库文件夹：使用 IndexedDB 独立 `folders` 表保存文件夹，默认存在“个人文件”；首页保留最近变动并以文件夹卡片列出其最近文档，空文件夹也可保留、进入和重命名。文件夹改名会同步更新文档归属。
- 文档编辑：文档打开后默认进入 Markdown 预览；预览正文限制为居中的 72ch 阅读列。编辑模式使用已有文件夹的下拉选择，停止输入 650ms 后自动保存，离开编辑器前再次保存。
- 使用统计与偏好：系统导航提供本地实时统计页，汇总待办完成率、待处理数、知识文档、文件夹、今日更新、正文字符和文件夹分布；偏好设置支持本地昵称、头像和主题选择，昵称与头像同步更新工作台身份展示。
